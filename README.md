
# ViewCounter — сервис подсчёта просмотров страниц

**ViewCounter** — это переиспользуемый backend-сервис для подсчёта просмотров страниц с защитой от накрутки.
Проект спроектирован как отдельный микросервис и может быть интегрирован в любые сайты и приложения (в том числе существующие проекты).

Сервис был создан как pet-project с фокусом на **чистую архитектуру, расширяемость и практики, применимые в production**.

---

## 🚀 Возможности

* 📈 Подсчёт просмотров для любых сущностей (страницы, статьи и т.д.)
* 🛡 Антифрод-механизм (защита от повторных просмотров)
* 🧩 Переиспользуемая архитектура (Domain / Application / Infrastructure / API)
* ⚙️ Лёгкая интеграция через HTTP API
* 🐘 PostgreSQL + Entity Framework Core
* 🔌 Готов к расширению (Redis, очереди, агрегация, аналитика)

---

## 🧠 Архитектура

Проект реализован по принципам **Clean Architecture**:

```
src/
 ├── ViewCounter.Domain
 │    └── ViewEvent (доменная модель)
 │
 ├── ViewCounter.Application
 │    ├── Commands / Handlers
 │    ├── Абстракции (Repositories, Services)
 │    └── Бизнес-правила
 │
 ├── ViewCounter.Infrastructure
 │    ├── EF Core (PostgreSQL)
 │    ├── Репозитории
 │    ├── Антифрод-политики
 │    └── Хэширование
 │
 └── ViewCounter.Api
      ├── Controllers
      └── HTTP Endpoints
```

### Почему так?

* **Domain** не зависит ни от чего
* **Application** содержит бизнес-логику
* **Infrastructure** можно заменить (например, PostgreSQL → Redis)
* **API** — тонкий слой для интеграции

---

## 🛡 Антифрод-логика

Просмотр считается **уникальным**, если:

* Совпадает `EntityType + EntityId`
* Совпадает хэш `(IP + UserAgent)`
* Не прошло 24 часа с предыдущего просмотра

Это позволяет:

* Защититься от автообновлений страницы
* Отфильтровать простую накрутку
* Не усложнять логику на стороне клиента

---

## 📡 API

### ➕ Зарегистрировать просмотр

**POST** `/api/views`

```json
{
  "entityType": "VisualNovel",
  "entityId": "123"
}
```

Ответы:

* `200 OK` — просмотр засчитан
* `409 Conflict` — повторный просмотр (антифрод)

---

### 📊 Получить количество просмотров

**GET** `/api/views/{entityType}/{entityId}`

```json
{
  "entityType": "VisualNovel",
  "entityId": "123",
  "totalViews": 1542
}
```

---

## 🛠 Технологии

* **.NET 9**
* **ASP.NET Core WEB API**
* **Entity Framework Core**
* **PostgreSQL**
* **Swagger**
* **Dependency Injection**

---

## ▶️ Запуск проекта

### 1️⃣ Клонировать репозиторий

```bash
git clone https://github.com/OnF1R/ViewCounter.git
cd ViewCounter
```

---

### 2️⃣ Настроить строку подключения

`ViewCounter.Api/appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=ViewCounterDb;Username=postgres;Password=postgres"
  }
}
```

---

### 3️⃣ Создать базу данных

```bash
dotnet restore
dotnet build
dotnet ef database update --project ViewCounter.Infrastructure --startup-project ViewCounter.Api
```

---

### 4️⃣ Запустить сервис

```
dotnet run --project ViewCounter.Api
```

Swagger будет доступен по адресу:

```
https://localhost:xxxx/swagger
```

---
