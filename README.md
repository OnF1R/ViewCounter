
# ViewCounter — сервис подсчёта просмотров страниц

**ViewCounter** — это переиспользуемый backend-сервис для подсчёта просмотров страниц с защитой от накрутки.
Проект спроектирован как отдельный микросервис и может быть интегрирован в любые сайты и приложения (в том числе существующие проекты).

Сервис был создан как pet-project с фокусом на **чистую архитектуру, расширяемость и практики, применимые в production**.

---

## 🚀 Возможности

* 📈 Подсчёт просмотров для любых сущностей (страницы, статьи и т.д.)
* 🛡 Антифрод-механизм (защита от повторных просмотров)
* 🧩 Переиспользуемая архитектура (Domain / Application / Infrastructure / API)
* ⚙️ Лёгкая интеграция через HTTP API
* 🐘 PostgreSQL + Entity Framework Core
* 🔌 Готов к расширению (Redis, очереди, агрегация, аналитика)

---

## 🧠 Архитектура

Проект реализован по принципам **Clean Architecture**:

```
src/
 ├── ViewCounter.Domain
 │    └── ViewEvent (доменная модель)
 │
 ├── ViewCounter.Application
 │    ├── Commands / Handlers
 │    ├── Абстракции (Repositories, Services)
 │    └── Бизнес-правила
 │
 ├── ViewCounter.Infrastructure
 │    ├── EF Core (PostgreSQL)
 │    ├── Репозитории
 │    ├── Антифрод-политики
 │    └── Хэширование
 │
 └── ViewCounter.Api
      ├── Controllers
      └── HTTP Endpoints
```

### Почему так?

* **Domain** не зависит ни от чего
* **Application** содержит бизнес-логику
* **Infrastructure** можно заменить (например, PostgreSQL → Redis)
* **API** — тонкий слой для интеграции

---

## 🛡 Антифрод-логика

Просмотр считается **уникальным**, если:

* Совпадает `EntityType + EntityId`
* Совпадает хэш `(IP + UserAgent)`
* Не прошло 24 часа с предыдущего просмотра

Это позволяет:

* Защититься от прямых запросов (`curl`, `Postman`)
* Отсечь автообновления страниц
* Минимизировать нагрузку на базу данных

---

## 📡 API

Сервис использует **двухшаговую регистрацию просмотра** (`ping → confirm`) для защиты от ботов, скриптов и прямых запросов.

---

### 🟡 1. Инициализация просмотра (Ping)

Создаёт временный токен, который должен быть подтверждён клиентом.

**POST** `/api/views/ping`

```json
{
  "entityType": "VisualNovel",
  "entityId": "123",
  "userId": "b278e5cd-d34c-49ad-a70a-2274b6b51609"
}
```

Ответ:

```json
{
  "viewToken": "QmFzZTY0RW5jb2RlZFRva2Vu"
}
```

📌 Токен:

* Хранится в кэше **10 секунд**
* Используется только один раз
* Нужен для подтверждения реального просмотра страницы

---

### ✅ 2. Подтверждение просмотра (Confirm)

Финально регистрирует просмотр, если токен валиден и антифрод-проверки пройдены.

**POST** `/api/views`

```json
{
  "viewToken": "QmFzZTY0RW5jb2RlZFRva2Vu"
}
```

Ответы:

* `200 OK` — просмотр зарегистрирован

```json
{ "message": "View registered" }
```

* `409 Conflict` — просмотр уже был засчитан ранее

```json
{ "message": "View already counted recently" }
```

* `200 OK` (игнор) — токен истёк

```json
{ "ignored": "expired" }
```

📌 При регистрации:

* IP берётся из `X-Forwarded-For` или `RemoteIpAddress`
* User-Agent берётся из заголовков
* Токен удаляется после использования

---

### 📊 Статистика просмотров

### 📈 Общее и уникальное количество просмотров

**GET** `/api/views/{entityType}/{entityId}`

Ответ:

```json
{
  "entityType": "VisualNovel",
  "entityId": "123",
  "total": 1542,
  "unique": 987
}
```

---

### 🕒 Последние просмотры

Возвращает `N` сущностей, которые просмотрели последними.

**GET** `/api/views/recent/{entityType}/{count}`

Пример:

```
GET /api/views/recent/VisualNovel/20
```

📌 Результат:

* Кэшируется на **10 минут**
* Уменьшает нагрузку при частых запросах

---

### 🔥 Самые популярные сущности

Возвращает топ `N` сущностей по просмотрам за последние **7 дней**.

**GET** `/api/views/popular/{entityType}/{count}`

Пример:

```
GET /api/views/popular/VisualNovel/10
```

Ответ (пример):

```json
[
  {
    "entityId": "123",
    "views": 542
  },
  {
    "entityId": "456",
    "views": 431
  }
]
```
---

### 💡 Почему именно `ping → confirm`?

* ❌ Нельзя просто дернуть API напрямую
* ❌ Нельзя накрутить просмотры через `curl`
* ✅ Только реальный клиент с JS может подтвердить просмотр
---

## 🛠 Технологии

* **.NET 9**
* **ASP.NET Core WEB API**
* **Entity Framework Core**
* **PostgreSQL**
* **Swagger**
* **Dependency Injection**

---

## ▶️ Запуск проекта

### 1️⃣ Клонировать репозиторий

```bash
git clone https://github.com/OnF1R/ViewCounter.git
cd ViewCounter
```

---

### 2️⃣ Настроить строку подключения

`ViewCounter.Api/appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=ViewCounterDb;Username=postgres;Password=postgres"
  }
}
```

---

### 3️⃣ Создать базу данных

```bash
dotnet restore
dotnet build
dotnet ef database update --project ViewCounter.Infrastructure --startup-project ViewCounter.Api
```

---

### 4️⃣ Запустить сервис

```
dotnet run --project ViewCounter.Api
```

Swagger будет доступен по адресу:

```
https://localhost:xxxx/swagger
```

---
